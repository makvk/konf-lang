let base_port = 8080
let timeout = 30

$[
    server: $[
        name: "nginx",
        port: !{base_port},
        timeout: !{timeout}
    ],
    features: list("gzip", "ssl", "cache"),
    sorted_features: !{sort(list("cache", "ssl", "gzip"))}
]